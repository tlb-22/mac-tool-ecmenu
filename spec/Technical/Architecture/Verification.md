# 架构验证记录

项目观察：2026-09-08 至 2026-09-11，macOS 26.6.2（25G83）、Xcode 26.6（17F113）、Apple Silicon。完整测试使用签名 Debug 主应用与 Finder Extension 测试宿主，可见交互检查使用独立 Preview。完整测试、签名 IPC、Debug 刷新、Release 与真实 Finder 的验收保留各自执行日期和证据范围；平台长期结论由对应 spec 维护。

## 自动化与集成

| 验证入口 | 实际结果 | 证据范围 |
|---|---|---|
| [完整测试](../../../scripts/Main.md#完整测试)（2026-09-11） | **231 项测试通过**；Preview 构建/注册、Finder 捕获工具静态检查、README 合成器构建、焦点恢复、环境切换与窗口检查通过 | 覆盖两端命令身份/注册、类型恢复、认证/传输、快照/副本、模板存储/提交/发布、原生编辑交接、命令平台边界与进度动作，以及命令/模板顺序持久化、无变化不保存、插入位置、取消、名称提交与排序衔接。菜单顺序通过 IPC、缓存和刷新保持，实际 NSMenu 按配置过滤后保序。完整测试前后 Finder 窗口 ID 集合一致 |
| [签名 IPC 集成](../../../scripts/Main.md#跨进程集成测试) | **2 场景通过**：配置查询、单次隐藏命令后观察 `UF_HIDDEN` | 当前真实开发签名 Sender 与主应用通信；同一测试前后 Finder 窗口 ID 集合一致 |
| [Release 构建](../../../scripts/Main.md#release-构建) | **1.1.0 (1)** Archive、ZIP 与 SHA-256 校验通过 | 主应用和 Finder Extension 均包含 arm64 / x86_64；版本、身份、权限、签名与 dSYM UUID 一致，ZIP 解压后签名复验通过。运行标识 `20260909-133516-release-34433`，正式产物位于 `.artifacts/releases/1.1.0+1/` |
| [刷新 Debug 扩展](../../../scripts/Main.md#构建与运行)（2026-09-11） | 当前主应用与 Extension 登记、启用与运行验证通过 | 路径指向仓库 `.derivedData/Build/Products/Debug/`；分别确认主应用与扩展进程。运行标识 `20260911-134657-run-debug-97348`，当前模板页的原生列表布局检查通过 |
| 真实 CopyPath（2026-09-08） | 签名 Sender 投递后，一般剪贴板的纯文本等于本轮 fixture 绝对路径 | 观察实际剪贴板变化，成功后恢复原全部条目/类型/字节并比较一致；原内容只在 probe 内存中保存。它覆盖成功路径，写入失败与等待 MainActor 时取消由注入测试覆盖 |

自动化覆盖的关键语义边界包括：剪贴板实际写入结果与取消、模板初始化/管理加载/提交的发布时机、提交后清理问题、配置保存先于通知、压缩参数确认顺序，以及呈现动作回到唯一进度所有者。文件位置变化由产品、测试、Preview 和集成 Sender 的实际构建共同检查。

模板页面测试显式查询本进程 AX 角色，再等待 SwiftUI 操作按钮就绪；初始化依据和能力限制见[原生页面测试宿主](../PreviewTarget.md)。英文、美国地区设置下单独执行全部 13 项页面测试通过，随后完整测试通过；`18af7ac` 的 [GitHub Actions 运行 34312979355](https://github.com/tlb-22/mac-tool-ecmenu/actions/runs/34312979355) 也已通过。

2026-09-11 完整测试运行标识 `20260911-134317-test-96517`，结果包 `20260911-134317-xctest-96517`。排序测试覆盖模板最新名称与内容引用保持、索引写入失败不提交、同位移动不写入或发布；原生页面检查系统列表容器、移动后的模板字段绑定，以及保存挂起时点击列表空白处更新最后目标。编辑会话测试以挂起任务验证放下等待同一次名称保存，并覆盖已失败结果不重试及失败恢复的同步原生通知；这些测试不向桌面发送鼠标拖拽事件，真实拖动证据见下节。

2026-09-09 完整测试运行标识 `20260909-133357-test-33564`，结果包 `20260909-133357-xctest-33564`；英文页面测试 `20260909-112700-ci-ui-tests-28290`；集成运行标识 `20260909-133452-ipc-integration-34275`；Debug 刷新 `20260909-003822-run-debug-8501`；剪贴板验收 `20260908-232154-clipboard-roundtrip-99607`。临时日志分别保存在 `scratch/logs`，测试和 probe 位于项目规定的对应 scratch 类别；本页的结果与限制是持久记录。

## 界面与真实 Finder

2026-09-11 简体中文独立 Preview 的两个设置页面通过[原生拖动诊断工具](../PreviewTarget.md#原生列表拖动诊断)完成真实鼠标输入检查：模板可以向下、向上移动；整行半透明预览随指针移动，原位行保留，蓝色插入线与左端圆圈标出落点。右键菜单页中不可用的 iTerm2 指令从最后移到第二位，显示开关的禁用不影响排序。八个模板的长列表在指针停留于下边缘时自动滚动。

名称交接检查确认：编辑名称后开始拖动会退出编辑并保存名称；随后按 Esc 取消拖动，名称保持已保存值、顺序保持原值；点击列表下方空白也会保存并结束编辑。

本次 Preview 运行标识为 `20260911-132117-preview-ui-93747`，诊断产物位于 probe `20260911-131445-rclick-motion-93355`；代表截图为 `native-name-cancel.png`、`native-upward.png`、`native-command-upward.png`、`native-edge-scroll.png` 与 `native-blank.png`。工具仅向明确 PID 的独立预览应用发送事件，窗口区域截图保存在仓库内。Preview 使用内存数据，以上结果证明界面拖动、名称交接与滚动行为；真实偏好与模板索引的持久化由上方业务边界测试分别验证，不能用 Preview 重排结果替代。

原生列表生命周期另以临时事件日志和真实鼠标输入核对：一次有效移动依次产生 `initial` 一次、`active` 多次、`onMove` 一次、`ended(move)` 一次。Preview 构建运行标识为 `20260911-133726-preview-ui-96042`，日志为同一 probe 中的 `lifecycle.log`；诊断打印已从源码移除。该观察限定于本页记录的 macOS 26.6.2 与当前 List 组合，调用方对名称任务的使用边界见[排序 API](../Runtime/CommandMenuSettings.md#设置列表排序交互)。

[批量 Preview](../../../scripts/Main.md#界面预览)完成通用设置、右键菜单设置、模板列表、压缩参数和多任务进度五种场景的英文/简体中文窗口截图，共 **10 张**。已逐张检查布局、文字、按钮、字段与进度显示，没有发现缺失或裁切；场景使用内存数据，不能据此证明用户偏好、真实登录或外部应用行为。运行标识 `20260908-222135-localized-previews-93640`。

[真实 TXT 菜单验收](../FinderMenuCapture.md#显式模板执行验收)观察到当前模板子菜单、点击 TXT 后生成零字节 `untitled.txt`，并由 helper 确认 Finder 选中新文件。验收输入与输出都在本轮仓库 fixture 中，模板和用户偏好未改写。运行标识 `20260908-223836-refactor-finder-95255`。

该次 TXT 命令与 helper 自身菜单/窗口清理成功，但外围窗口集合检查报告 **新增一个 Finder 窗口 ID、没有丢失原窗口**，因此整条包装命令退出失败。此记录不能作为“真实菜单验收前后窗口集合不变”的通过证据；新增窗口的来源未确认，也没有据此关闭用户窗口。此前完整测试与签名 IPC 测试的窗口检查通过记录分别成立。真实菜单的窗口与焦点条件见[Finder 窗口保持](../FinderWindowPreservation.md)与[捕获边界](../FinderMenuCapture.md)。

## 文档与复核

2026-09-11 排序相关的 4 张流程图渲染并逐张阅读通过；11 份修改 spec 中的 226 个本地链接/锚点及 27 张表格检查通过。图表运行标识 `20260911-125524-settings-order-diagrams-89826`。

2026-09-08 对长期 Technical 中的 **27 张 Mermaid 图完成渲染验证**。图使用进程分区、模块生命线或活动分区；模块邻接表注明内部输入/输出、外部 API、完成点与失败。规范与能力入口由 Technical 维护，源码链接与导航随目录同步。系统总图与新建文件时序图另完成截图阅读检查。2026-09-09 的文档快照包含 62 份 Markdown，574 个本地文件链接与章节锚点校验通过，格式检查通过。

独立源码复核未发现本轮行为回归：认证先于正文及文件描述符释放顺序保持；Extension 的单次拉取与过期响应处理保持；模板草稿和原生控件身份保持；关闭设置窗口不释放命令任务；参数确认保存顺序与任务取消语义保持。

文件头静态检查覆盖 226 个 Swift、27 个 shell、2 个 Python 与 1 个 CI YAML 文件，共 256 份；逐文件职责说明已复核。核心界面入口集中于 Technical 第一级的 `ViewCatalog.md`。本次命名验证保留并检查已有存储目录、偏好键、JSON 字段、通知标识与本地化资源；IPC 查询的固定编码值由测试直接断言。

2026-09-09 的模块映射与截图工具检查覆盖 43 份 Technical 和开发脚本文档中的 624 个本地链接及锚点、64 张表格；28 张 Mermaid 图渲染通过，并阅读检查新建文件、命令执行和截图工具流程。截图工具 `--check` 的场景、fixture、helper 构建与静态身份检查通过，运行标识 `20260909-164713-finder-menu-capture-27239`。命令行的用法错误、无效模板输出名、已有目标文件和已有截图路径四个边界检查通过，均在 UI 操作前失败并保留已有文件，运行标识 `20260909-164947-capture-cli-boundaries-27692`。本次工具验证未重新执行真实 Finder 点击和截图，GUI 行为的实机证据范围仍以上文和捕获专题中的记录为准。

## 尚未覆盖的实机边界

本次未重新执行注销/登录、系统批准登录项、所有输入法/Space/模态窗口组合、外置卷/只读卷、VS Code/iTerm2 实际加载，以及完整双语 Finder 多选/侧边栏场景。对应 spec 保留既有带版本的观察、自动化覆盖与可复现人工验收步骤；它们不被本次运行记录扩展为新系统版本的保证。
