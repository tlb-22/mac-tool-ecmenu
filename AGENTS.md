# 项目工作规范

## 代码设计

- 强类型建模：使用领域类型表达有效状态和可预期的运行时失败，避免以字符串、布尔值、字典或重复字段维持隐含约定；让不可能状态在类型上不可构造，而非交由后续流程反复检查。
- 跨端边界对齐：跨模块和跨进程契约的路径结构、请求、结果与失败语义保持对应。
- 副作用隔离：系统副作用留在明确边界，内部流程使用纯函数传递不可变事实。
- 最小可变源：每份可变状态具有唯一所有者和明确生命周期；只保存不能由其他状态推导的源状态，其余状态按需推导。
- 结构化目录：按一起变化的业务能力组织模块、目录与文件，使目录结构反映职责边界。
- 高内聚拆分：当内容本身已经复杂，或具有独立职责、可独立理解、可共享复用时主动拆分；仍然简单、相互依赖且需要共同理解的内容保持在一起，不因子主题或文件长度机械拆分。
- 不滥用防御性编程：只对实际运行中可能发生的失败进行必要处理；对仅由实现错误产生的不可能状态，严格禁止静默兜底，也严格禁止保留旧版本、旧实现的兼容路径；持久化数据升级由独立迁移完成。

## 项目文档

- 渐进展开的文档编写模式：从各目录的 `Main.md` 开始阅读；它只保留总述、关键结论和导航，细节内容按高内聚原则组织。
- 文档语言的马尔可夫性/禁止负向回声：以最新确认的要求作为唯一基线，从最终实现重新生成成品；成品面向未见过本次会话的读者，只陈述当前成立的事实；会话中的旧方案、纠正和中间尝试只作为修改控制信息，而不是正文素材。
- 待办与提案：TODO 只记录待完成工作，完成后删除；暂不实施但需长期保存的设计归入 `spec/Proposals/`。
- 需求与技术文档：`spec/Requirements/` 记录产品行为与验收边界，不记录具体视觉细节选择，例如视觉参数/图标名称；`spec/Technical/` 记录外部契约、项目实测、跨模块约束和无法从源码可靠恢复的设计动机。源码已清楚表达的实现不重复写入文档。
- 调查结论/实测经验沉淀：对反复出现、经历多轮方案验证，或需通过 API 与外部文档调研、项目实测才能确定的问题，主动补充 spec/Technical/；简单、一次性且无复用价值的问题不记录。只记录稳定路径、关键失败路径及原因、或影响实现判断的 API 行为，不按时间顺序记录调查过程；平台结论须区分官方契约、项目观察与推断，并说明验证版本与证据限制。
- 文档维护：行为或技术边界变化时同步更新对应需求、技术文档和链接，并删除被替代的重复内容。
- 外部项目分析：`reference/<项目>/` 保存注明版本的外部源码快照，其 `.Analysis/` 只分析该项目；本项目的采用与取舍写入 `spec/Technical/`。

## Git 操作

- 除非用户临时授权，否则自动化代理不得执行 `git add` 或以其他方式修改 Git 暂存区。

## 构建、测试与产物

- 项目相关的构建、测试和运行产物必须留在仓库内，不使用系统临时目录保存它们。
- `.derivedData/` 只作为 Xcode Derived Data 根目录；脚本可以读取其中的构建产品，但不得在其中创建自定义日志、测试 fixture、探针、截图或发布物。
- `.artifacts/scratch/` 只保存一次运行或调查产生的可重建内容，按 `logs/`、`tests/`、`probes/`、`previews/` 分类。单次运行产物的顶层文件或目录使用 `YYYYMMDD-HHMMSS-<purpose>-<pid>` 命名；没有相关任务运行时，整个 `scratch/` 均可删除。
- 可重复执行的测试与预览定义维护在 `Tests/`（预览宿主位于 `Tests/ECMenuPreviews/`），诊断入口维护在 `scripts/`；源码、正式配置和持久文档不得依赖 `scratch/`，需要保留的结论写入相应文档。
- 正式交付物位于 `.artifacts/releases/<version>+<build>/`；已有的非空版本目录不得静默覆盖。
- 日常 Debug 构建与运行使用 `./scripts/run-debug.sh`，完整测试使用 `./scripts/test.sh`；共享 `.derivedData/` 或 Finder 登记状态的脚本顺序执行。删除 `.derivedData/` 前，先停止并注销从其中运行的应用和 Finder Extension。
- 图标变化但程序坞仍显示旧缓存时使用 `./scripts/run-debug.sh --refresh-icon`；Finder Extension 源码变化、菜单消失或扩展未加载时使用 `./scripts/run-debug.sh --refresh-finder`。

## Finder Extension 调试

- Extension 是否启用、是否登记、是否运行是三个独立状态；菜单消失时先区分加载失败与菜单代码返回空结果。
- 重新构建后须确认系统登记和运行进程都指向 `.derivedData/Build/Products/` 中的当前 Debug 产物；Finder 持有旧进程时执行完整刷新并再次验证，不能只重新打开主应用。