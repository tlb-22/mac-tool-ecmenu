# 待办

只记录已经确认但尚未完成的工作；完成后删除对应行。

## 新建 TXT -> 新建文件

- 新建 TXT 改为“新建文件”，可以由用户自由添加文件模版类型：右键菜单下改为“新建文件”命令，设一级子菜单，每个子菜单对应一种文件，在 status page 中左侧栏添加一个选项卡，可以添加文件模版（保存到软件管理的一个文件夹内）、设置该文件名了显示名（TXT，默认为文件名扩展名）、默认文件名（例如untitled.txt，重复时按照和之前新建文件同样的重命名规则，例如untitled_copy.txt）。